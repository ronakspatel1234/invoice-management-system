/**
 * @author - Ronak Patel.
 * @description -Create for auth model type.
 * @prop email - type string.
 * @prop password - type string.
 */
export class Auth {
    id: number;
    email: string;
    password: string;
}
